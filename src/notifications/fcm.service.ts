// Superseded by FirebaseModule / FirebaseService.
// Kept as a re-export alias so any future imports of FcmService continue to compile.
export { FirebaseService as FcmService } from '../firebase/firebase.service';
export type { FcmSendResult } from '../firebase/firebase.service';
